# Code review, second pass (gap hunt) — 2026-07-20

Follow-up to [code-review-2026-07-20.md](code-review-2026-07-20.md) (pass 1: H1–H7,
M1–M10, L1–L14 — all H/L resolved on `dev`, M tier open). This pass deliberately
covered what pass 1 structurally could not: the **submodule fork changes** (first
review ever), the **Java↔native seam and Android lifecycle**, an **adversarial
re-sweep** of hot paths hunting for what pass 1 missed, and a **public-repo
hygiene/docs audit**. Four independent review agents; nothing below duplicates
pass 1.

**Verification column**: `review` = found by a pass-2 agent with source citations,
not yet independently re-confirmed — confirm before fixing. `corroborated` =
independently matches the standalone 2026-07-19 plume review, i.e. two separate
reviews reached the identical finding.

**Release-state fact established during this pass:** `v1.0.0` AND `v1.0.1` both
shipped from `main` with NONE of the pass-1 fixes (all are `dev`-only) and with
P1 below. The next release tag carries the entire accumulated fix set.

---

## HIGH — fix before next release

> **Status 2026-07-21:** all five HIGH items verified against source and FIXED on
> `dev` (uncommitted at time of writing; build-verified — `assembleDebug`
> **BUILD SUCCESSFUL**, 0 errors, all changed TUs recompiled for arm64). P1+S4
> were fixed together as one ANativeWindow ownership invariant (see below); the
> plume half is a **submodule change** (fork push + pointer bump still pending).
> Per-item resolutions are appended to each row.

| # | Verified | Location | Issue |
|---|----------|----------|-------|
| P1 | VERIFIED REAL — FIXED | `lib/rt64/src/contrib/plume/plume_vulkan.cpp:2549-2556` | **The 2026-07-19 review's UAF was never fixed and shipped in v1.0.0/v1.0.1.** `recreateSurface()` failure path: old surface already destroyed (dropping libvulkan's last ref on the OLD ANativeWindow), `pendingRenderWindow` already consumed; if `vkCreateAndroidSurfaceKHR` fails, `desc.renderWindow` still points at the freed window and the present loop (`rt64_present_queue.cpp:468`) calls `getWindowSize()` → `ANativeWindow_getWidth(freed)` at frame rate. Background/resume is a constant user action. Fix (~3 lines, per the 07-19 review): commit `desc.renderWindow = newWindow` before the create attempt, or re-stash `newWindow` into `pendingRenderWindow` on failure. **Fix together with S4.** **→ FIXED:** `recreateSurface` now adopts `newWindow` into `desc.renderWindow` (and releases the replaced window's ref) BEFORE the surface teardown/create, so the failure path derefs a live window we own a ref on, never the freed old one. Confirmed the exact mechanism against source (`needsResize()`→`getWindowSize()`→`ANativeWindow_getWidth(desc.renderWindow)` runs every present-loop iteration at `rt64_present_queue.cpp:468`). Part of the ownership invariant below. **DEVICE-VERIFIED 2026-07-21** (RP5) via an automated lock/unlock stress harness (`docs/testing/resume-stress.sh`): 30 slow + rapid cycles, 33 surface recreations across 5 distinct ANativeWindow ptrs, pid constant, ZERO SIGSEGV/tombstone, saves byte-identical, game renders after (screenshot). Caveats: the `vkCreateAndroidSurfaceKHR`-fails branch wasn't empirically forced (surface creation succeeded every cycle — correct-by-construction only); programmatic rapid lock/unlock races the RP5 keyguard, producing transient `VK_ERROR_SURFACE_LOST`/`NATIVE_WINDOW_IN_USE` swapchain retries absorbed by the resize loop (NOT crashes, NOT recreateSurface fails). |
| S4 | VERIFIED REAL — FIXED | `src/main/rt64_render_context.cpp:44-50`, `src/game/input.cpp:353-359` | **Resume-window handoff holds no ANativeWindow reference** — `android_publish_resume_window` stores a raw pointer with no `ANativeWindow_acquire`, while SDL's `surfaceDestroyed` waits a bounded 500 ms then releases its ref regardless. Rapid background→foreground flips can free a window the swap chain still holds (or one published-but-not-yet-adopted); the eventual `vkCreateAndroidSurfaceKHR` runs on freed memory. Fix: acquire at publish, release on overwrite (`exchange`, not `store`) and after adoption. Host-side half of P1's lifetime problem; plausible root cause of background/resume SIGSEGV reports. **→ FIXED:** implemented a single **ownership invariant** spanning host↔plume: every non-null window in a slot (`g_pending_resume_window` → plume `pendingRenderWindow` → `desc.renderWindow`) carries exactly one ref we own. Publish acquires + release-on-`exchange`; the ref is handed down the slot chain; the swap-chain ctor acquires the construction window (removes the first-window asymmetry) so `recreateSurface` can always release the window it replaces; the dtor releases what remains. SDL's ~500 ms release premise corroborated in-tree by the existing plume comment (`plume_vulkan.cpp:2510,2525`). All transfers use atomic `exchange`; refcount balances under races (traced, incl. same-pointer reuse). |
| S1 | VERIFIED REAL — FIXED | `src/game/input.cpp:183-192` × `SDLActivity.java:598-611` | **Activity destroy wedges the app when in-game.** On Android `SDL_QUIT` is delivered only by `SDLActivity.onDestroy` → `nativeSendQuit()`, which flushes the event queue first — then `sdl_event_filter` answers it by opening the Quit/Cancel prompt (`ui_config.cpp:238-253`), which can never be answered. `onDestroy`'s unbounded `mSDLThread.join()` blocks the UI thread until the system SIGKILLs; no orderly save-thread join; everything after `super.onDestroy()` in MainActivity (restart dispatch, halt) is unreachable. Fix: on `__ANDROID__` treat `SDL_QUIT` as immediate `ultramodern::quit()`; keep the prompt desktop-only. **→ FIXED** exactly as recommended (`input.cpp` SDL_QUIT handler, `#if defined(__ANDROID__)` → `ultramodern::quit(); return true;`, prompt path kept desktop-only). Confirmed the prompt's own "Quit" button calls `ultramodern::quit()` (`ui_config.cpp:245`), so immediate quit is behaviorally identical minus the un-answerable modal; and that no in-app path pushes `SDL_QUIT` on Android (only SDL's `onDestroy`). |
| N1 | VERIFIED REAL — FIXED | `src/game/config.cpp:94-105,154-157,216-228,471-475` | **Parseable-but-wrong-typed config JSON → uncaught `nlohmann::type_error` → startup crash loop.** `read_json` catches only `parse_error`; a wrong-typed value (`"rumble_strength": "25"`, `[null]` binding, missing `input_id`) throws through `load_*_config` with no catch up to `main` → `std::terminate` pre-window. `.bak` is a byte-identical rotation → also throws; the reset-to-defaults path (`config.cpp:535-553`) is never reached; only Clear Data recovers. Reachable via corruption, hand edit, or downgrade. Fix: try/catch around each loader body falling through to the existing reset path. **→ FIXED:** each of the four `load_*_config` bodies now wraps its deserialization in `catch (const nlohmann::json::exception&) → return false`, routing into `load_config`'s pre-existing per-loader reset-to-defaults + save path. `json::exception` is the base of `type_error`/`out_of_range`, so all wrong-type/missing-key throws are caught; the partially-built value is not applied (e.g. graphics `set_graphics_config` stays outside the try). |
| N2 | VERIFIED REAL — FIXED | `src/game/input.cpp:127-128,200-207` → `src/ui/ui_config.cpp:145-155` | **Binding-scan cancel mutates RmlUi data models from the event thread** (`DirtyVariable` on three model handles, no lock, no queue) while the render thread runs `Context::Update()` (`ui_state.cpp:786`) — concurrent container mutation → heap corruption. Also unsynchronized: `scanned_input_index`/`scanned_binding_index` and event-thread reads of binding arrays during render-thread writes. Fix: route cancel through the existing `ui_event_queue`. Not covered by pass-1 M6/M9. **→ FIXED:** the two event-thread cancel sites (`input.cpp` keyboard-Escape + controller-menu-toggle) now call `request_cancel_scanning_input()` — an atomic-only deferral (`scan_cancel_pending`) that stops scanning immediately; the UI thread drains it via `poll_scan_cancel_requested()` in `draw_hook`, right beside the existing finish poll and before `Context::Update()`, and calls the RmlUi-touching `cancel_scanning_input()` there. This mirrors the already-correct finish path (event thread sets an atomic; UI thread does the DirtyVariable). **Secondary race also improved:** cancel was previously the only event-thread *writer* of `scanned_input_index`/`scanned_binding_index`; after this, all writes are UI-thread, leaving only the event-thread *read* at `input.cpp:223,242` — an aligned 32-bit load, single-writer-thread, one-frame-stale at worst (benign; not a crash). Those statics are `Bind<int>`'d into the RmlUi model by raw address, so making them `std::atomic` is not free — left as documented-benign rather than fixed. |

## MED

| # | Verified | Location | Issue |
|---|----------|----------|-------|
| N3 | VERIFIED REAL — FIXED | `include/goemon_config.h:33,79,106,128,144`; `include/recomp_input.h:207` | **Every On/Off enum fails OPEN**: `NLOHMANN_JSON_SERIALIZE_ENUM` maps unknown/wrong-typed values to the FIRST map entry, and `On` is listed first everywhere. Corrupt/downgraded config silently enables autosave (inverting the settled Off default), cheats, analog cam. Upstream counterpart: unknown `res_option`/`rr_option` → `Original` (loses tuned Android defaults). Fix: safe value first in each map. **→ FIXED:** reordered the five dangerous On/Off maps (Autosave, AnalogCam, Cheat, SwapWhileMoving, AttackWhileMoving) to put **Off first**, so the unknown-value fallback equals the setting's own default. Reordering only changes the fallback — known string↔enum pairs and the enum's integer values are untouched (verified against the macro's `to_json`/`from_json` semantics). `BackgroundInputMode` deliberately LEFT On-first (its default is On; a comment now says so) — the principle is "fallback = the setting's default," not "Off everywhere." **Not done:** the upstream `res_option`/`rr_option`→`Original` counterpart is in the N64ModernRuntime submodule + low-impact (wrong-*type* now hits N1's full graphics reset); left as a follow-up. |
| N4 | VERIFIED REAL — FIXED | `config.cpp:285-294`; `input.cpp:776-808,636`; `main.cpp:239`; `camera.c:252-255,269` | **No range clamping between config JSON and consumers.** `joystick_deadzone: 100` → 0/0 = **NaN** stick (NaN→int is UB, flows into guest input + camera); >100 inverts axes; rumble >32767 signed-overflow UB, negative wraps to near-max; `main_volume: 10000` = 100× amplification; huge cam sensitivity → float→s32 overflow UB. Fix: `std::clamp` at each load site. **→ FIXED:** clamped all nine numeric setters in `ui_config.cpp` to `[0,100]` (rumble, gyro, mouse, deadzone, analog-cam X/Y, main/bgm/se volume) — the setters are the single chokepoint for both the UI sliders and config load, so the clamp bounds hand-edited values before any consumer. Confirmed the deadzone NaN mechanism (`input.cpp:819` divides by `1 - deadzone/100`) and added a belt-and-suspenders guard in `apply_joystick_deadzone` for the exact-100 (divisor 0) endpoint. Cam-sensitivity consumers in `camera.c` read the now-clamped host value, so they need no separate patch. |
| C2 | VERIFIED REAL — FIXED | `android/gradle/wrapper/*`, both workflows | **gradle-wrapper.jar committed, never integrity-verified; no `distributionSha256Sum`.** A tampered wrapper executes with keystore + ROM PAT in scope — the H7 hole class, still open for the wrapper. Fix: `gradle/actions/wrapper-validation` first step after checkout in both workflows + pin the distribution SHA. **→ FIXED:** added a SHA-pinned `gradle/actions/wrapper-validation@ed408507…  # v4` step after checkout in BOTH workflows, and pinned `distributionSha256Sum=4159b938…` (the official `services.gradle.org` checksum for gradle-8.0-bin.zip) in `gradle-wrapper.properties`. Action SHA resolved from `v4` via the GitHub commits API; distribution hash from the official raw `.sha256` file. |
| C1 | VERIFIED REAL — FIXED | `.github/workflows/android.yml` | **No `permissions:` block** — debug workflow token gets repo default (potentially write-all) while running third-party actions + full Gradle build. Needs exactly `permissions: contents: read`. (`android-release.yml` already sets minimal `contents: write`.) **→ FIXED:** added top-level `permissions: { contents: read }` to `android.yml` (artifact upload needs no repo write). Release workflow already had `contents: write`; left as-is. YAML re-parsed clean. |
| C3 | VERIFIED REAL — FIXED | `.github/workflows/android-release.yml:23-26` | **Any `v*` tag on ANY commit produces a signed public release** — no ancestry guard. A stray/sprayed tag on a WIP commit ships unreviewed signed code. Fix: `git merge-base --is-ancestor "$GITHUB_SHA" origin/main \|\| exit 1` (fetch-depth already 0). Nit: v1.0.0 annotated, v1.0.1 lightweight — standardize on annotated. **→ FIXED:** added a "tag commit must be on main" guard step right after checkout — `git fetch --no-tags origin main` then `git merge-base --is-ancestor "$GITHUB_SHA" FETCH_HEAD` (FETCH_HEAD, since checkout only fetched the tag ref), failing the release if the tag isn't on main. The annotated-vs-lightweight nit is not addressed (process, not code). |
| S3 | review | `src/game/config.cpp:564` (also `:530`; `pi.cpp:258`; `recomp.cpp:87-88`) | **Throwing `create_directories` on the SD path**: card removed mid-game + close settings menu → uncaught `filesystem_error` → SIGABRT. Violates the settled "writes fail cleanly" design the save chain honors (`pi.cpp:135-159`, `files.cpp:31-51`, `save_rollback.cpp` are all clean). Fix: `std::error_code` overloads + early return. |
| S2 | review | `AndroidManifest.xml:44`; `android_glue.cpp:88-102`; `MainActivity.java:115-129` | **Same-process recreation of MainActivity is unsupported by the latched native side and neither prevented nor recovered.** `configChanges` still misses `fontWeightAdjustment` (Android 12 Bold text — H3's trigger family), `mcc\|mnc`, `colorMode`, `grammaticalGender`; on recreation a second `nativeInit` dies at the duplicate-symbol guard with a misleading "syms.ld" modal (or black-screens on instant `exited`). Fix: add the config bits; make the non-finishing destroy branch also route through process replacement (or detect second-init-in-latched-process → RestartActivity). |
| N5 | review | `src/game/controls.cpp:212-216`; `patches/camera.c:176` | **R-mask is mode-global but the analog camera is engine-scoped**: with analog cam On, native N64 R is suppressed everywhere — including Impact battles/minigames/menus (different engine modules where the analog cam does nothing) that may natively read R. Needs a device check of R usage in non-field engines; the structural asymmetry is the defect. |
| N6 | review | `src/ui/ui_state.cpp:438-440,538-549,884-902` | **Inconsistent `ui_state` lifetime guards**: several entry points deref with no mutex and/or no null check (`activate_mouse` reachable from the event thread via SDL_QUIT racing `deinit_hook`; `queue_image_from_bytes_*` callable from the guest thread during render-thread teardown). Fix: apply the guarded pattern at `:844-882` uniformly. |
| N7 | review | `src/game/input.cpp:149,269,288` | **Sensor/ADDED handlers resurrect or leak controller entries**: `operator[]` on a queued sensor event after REMOVED re-creates a zombie entry; ADDED for an already-present instance id overwrites `.controller` without closing the old handle (the H2 leak class, one branch over). |
| P2 | review | `lib/N64ModernRuntime/ultramodern/include/ultramodern/ultramodern.hpp:59-74` | **Write-callback ordering guarantee overclaimed**: an in-flight flush can snapshot a write whose callback hasn't fired yet (guest memcpy'd into the buffer, preempted before `notify_save_write`). Parent's save_rollback usage is safe (late dirty-mark = back up next flush) but the documented contract is stronger than the code. Fix: weaken the comment (or publish a buffer generation counter). |
| B1 | review | `android/app/build.gradle:59`; `BUILDING.md:94`; `README.md:133` | **`GOEMON_ANDROID` is defined and documented but consumed by nothing** (CMake gates on the NDK's `ANDROID`; the recent build-log warning said so). Docs teach a lie. Drop it everywhere or consume it. |
| D1 | review | `docs/autosave.md` ~295-299 | **autosave.md still teaches the false "non-recursive mutex → self-deadlock" rationale** that the L14 fix corrected in ui_saved_indicator.cpp, ui_state.cpp, and CLAUDE.md — this doc was missed. Same supersession-hygiene class; the doc is load-bearing. |
| D2 | review | `docs/autosave.md` ~841-844 vs ~1000 | **Internal contradiction on `.manual.bak`**: "implemented and not yet verified on device" vs the same file's "RUN AND PASSED on device, 2026-07-19". Stale callout fails safe but destroys trust in the doc. |
| C4 | review | (no `.github/dependabot.yml`) | **Dependabot coverage is accidental**: only default npm security PRs against the dev-only SCSS toolchain (5 open, unbuilt — no `pull_request` CI). Zero coverage for `github-actions` (SHA pins now age silently), `gradle`, `gitsubmodule`. Add dependabot.yml scoping github-actions + gradle; triage the 5 PRs. |
| D5 | review | `docs/input/n64-goemon-input-assignment.csv` | **The CSV `input.cpp:473` cites as the authored source of truth carries dead contradictory columns** (secondary cols rotated one C-button off; cols I-J are an abandoned draft layout; keyboard col disagrees with code). Primary cols A–C verified correct vs shipped defaults. Strip or mark the dead columns. |

## LOW

| # | Verified | Location | Issue |
|---|----------|----------|-------|
| P3 | corroborated | `plume_vulkan.cpp:2562-2567` | `presentSupported==false` path incoherent (surface stays bound, next resize builds on it anyway; `vkGetPhysicalDeviceSurfaceSupportKHR` VkResult unchecked). |
| P4 | corroborated | `plume_vulkan.cpp:~3080,~3635` | Null-bind guards log per-bind → logcat spam at frame rate on exactly the weak devices they protect. Log once per object. |
| P5 | corroborated | `plume_vulkan.cpp:~1400` | `static int s_computePipelineIndex` non-atomic increment; make atomic. Plus nit: `recreateSurface` reuses construction-time surface format/alpha/present-mode picks (deserves a comment). |
| P6 | review | `lib/rt64/CMakeLists.txt` | SDL2 FetchContent pinned to tag `release-2.32.8`, not a SHA (H7's moved-tag exposure, in the fork); also ~120 changed lines editing four shared shader functions = merge friction on upstream pulls. |
| P7 | review | `lib/N64ModernRuntime/ultramodern/src/events.cpp:29 vs 228` | Duplicate `extern std::atomic_bool exited;` declaration (cosmetic). Also noted: upstream `files.cpp` save rotation is non-atomic copy_file with no fsync and `.bak` fallback only on open-failure (upstream defect; bounds what any rollback promise can claim). |
| S5 | review | `MainActivity.java:32-35` | Static `System.loadLibrary` preempts SDLActivity's graceful broken-library dialog; load failures hard-crash instead. Catch `UnsatisfiedLinkError`, flag, dialog. |
| S6 | review | `android_glue.cpp:48-51`; `MainActivity.java:105-107` | Restart-latch comment names the wrong thread (atomic saves it); `nativeDestroy`/`nativeGetRestartTarget` run before the SDL-thread join — harmless while nativeDestroy is a no-op, latent trap if it ever does real teardown. Comment it. |
| N8 | review | `ui_state.cpp:584,718-741,343-346` | Two menu-toggle events in one frame → duplicate `show_context` → release-build error dialog; config menu can also open on top of the quit prompt. |
| N9 | review | `patches/camera.c:711-729,314-324` | Audio-pan hook can perform the one-shot azimuth capture from its *argument* camera on the first engaged frame; a non-live camera in that window poisons the whole engagement. |
| N10 | review | `patches/autosave.c:656-658,691-693` | Status printfs deref `G_SAVE_CTX_PTR` when null (masked RDRAM read prints garbage slot) — the log lies exactly when diagnosing a failure. (Pass-1 patches review also flagged this; never fixed.) |
| N11 | review | `input.cpp:666-667,1046` | `abs(INT32_MIN)` UB on hostile `input_id` from hand-edited controls.json (L5's family, one layer deeper); also `controls.cpp:112` dead deadzone local. |
| N12 | review | `config.cpp:382-395` | `reset_graphics_options` omits `api_option` (survives by value-init coincidence). |
| N13 | review | `ui_config.cpp:800` | Unexplained 50 ms render-thread sleep in `make_graphics_bindings` — either a masked init race (still there on slow devices) or dead time. (Pass-1 also noted; never resolved.) |
| N14 | review | `patches/autosave.c:186-224` | Timed autosave not gated on the recomp menu being open — a 2-min boundary can commit mid-settings-toggle. Coherent save (input zeroed); behavioral, against the gate's intent. |
| N15 | review | `ui_state.cpp:423-429` | `update_contexts` range-fors `shown_contexts` while dispatching arbitrary update handlers; a handler calling show/hide_context invalidates the iterator. No in-tree offender today. |
| N16 | review | `src/game/recomp_api.cpp:56-61` | `60 / frame_divisor` with no zero check; registered + declared with no guest caller. Latent SIGFPE. One-line clamp. |
| R1 | review | `android-recomp/`, `android-ui-probe/` | Committed throwaway probe projects that self-declare "Discard once folded into root"; mirrored defs drift silently. Delete or retitle as historical. |
| R2 | review | `CMakeSettings.json`, `launch.vs.json`, `.github/linux/`, `.github/macos/` | Desktop/VS scaffolding in an Android-only public fork ("no supported desktop build" per BUILDING.md:4). NOTE: `.github/macos/*` IS referenced by root CMakeLists' APPLE branch — keep-or-kill decision, not blind deletion. |
| B2 | review | `patches/Makefile:7` | `CC ?= clang` never fires (make predefines CC=cc); bare `make -C patches` uses host cc with cryptic errors. `CC := clang` or document. |
| B3 | review | `CMakeLists.txt:559-563` | Configure-time symlink written into the source tree every configure (stale after build-dir switches; fails on Windows without dev mode). Cosmetic. |
| B4 | review | `README.md:128-135` | README build section omits ROM requirement + host codegen and never links BUILDING.md → newcomers hit a baffling link failure. One line fixes. |
| C5 | review | `android.yml:118-123` | Debug APK artifacts at default 90-day retention; `retention-days: 14` is plenty. |
| C6 | review | `android.yml:16` | Stale header comment ("release signing is a later addition") — it has existed since v1.0.0. |
| D3 | review | `docs/autosave.md:26` | References nonexistent `AUTOSAVE_TEST_COMBO` define (actual: `AUTOSAVE_LZ_MASK`). |
| D4 | review | `docs/re-notes/README.md:150` | Index line contradicts RESUME-menu-framerate.md's own SUPERSEDED banner — resumes a closed investigation. |
| P-pii | review | `CLAUDE.md:5,13` | "(PRIVATE)" is stale on a public repo (one-word fix). Personal Gmail in CLAUDE.md + 59 historical commits is a **documented-accepted** exposure (recorded at CLAUDE.md:13-14) — listed for completeness, not action. |

## Verified clean (hunted, nothing found — do not re-derive)

- **PII sweep**: zero hits for username/home paths/IPs/tokens/keys/serials in all tracked files and all post-flip commit messages. The pre-flip scrub held.
- **Submodules**: all three fork tips pushed and gitlink-consistent (rt64 8c73b3f, N64MR b6f6253, plume 65783a0); `.gitmodules` chains correct; CI/cloners safe.
- **Host-API contract**: 36 syms.ld entries unique/orderly, generated table drift-proof by construction, missing impl = link error, duplicate = startup hard-fail, unregistered call = loud runtime fail, + the L1 guard. Four independent layers.
- **JNI**: string handling correctly paired; `g_data_dir` happens-before via Thread.start(); restart contract holds (saving thread joined before `game_main` returns; `finish()` nulls `mSDLThread`).
- **Backgrounded behavior**: fully parked (VI on CV, pump blocked in SDL, audio paused); quit-while-backgrounded cannot deadlock (single-write-site `exited` under the predicate mutex; pause cleared in `quit()`).
- **SD save chain** fails cleanly as designed (`pi.cpp` good()-checks, `files.cpp`/`save_rollback.cpp` error_code-based) — S3's config chain is the only violator found.
- **Config recovery** for *unparseable* JSON works (reset path); 2→3 binding migration correct; unknown keys ignored.
- **Autosave**: monotonic clock (wrap-correct), settle-check TOCTOU-free (single guest call), rollback bracket covers all early returns, printf formats match.
- **Camera math**: binangle wrap by design; no float accumulation (eye recomputed from immutable captured unit vector); zoom clamped; all normalizations guarded; the pass-1 pitch-clamp corner **proven unreachable** (total elevation bounded (−112.5°, +135°) vs the ~180°±10° hole).
- **rt64 fork changes**: EndianSwapUINT16 rewrite verified algebraically; `swapChainValid` present-loop guard coherent; Adreno descriptor-pool trade-off documented.
- **N64ModernRuntime pause gate + VI seeding**: structurally correct, single write site, invariant documented — the strongest code in the fork set.

## Recommended order

1. **P1 + S4** (the lifetime pair — shipped crash class on the background/resume path everyone uses).
2. **S1, N1, N3, N4** — small crash-or-integrity fixes.
3. **C2, C1, C3** — three cheap workflow edits closing the remaining supply-chain/release-integrity gaps; land before the next tag.
4. **S3, S2**, then **D1/D2** with the M-tier backlog; everything else opportunistically.

Next release note: v1.0.0/v1.0.1 contain none of pass 1's fixes and all of P1 —
once items 1–3 land on `dev`, cut `v1.0.2` (via a `v1.0.2-rc1` dry-run tag first
to exercise the new L8 versionCode derivation).
